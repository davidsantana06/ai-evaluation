{% macro render_th(icon, label, description = none) %}
    <th>
        <i class="{{ icon }} fa-fw mr-1"></i>
        <span
            {% if description %}
                class="has-tooltip"
                data-tooltip="{{ description }}"
            {% endif %}
        >
            {{ label }}
        </span>
    </th>
{% endmacro %}